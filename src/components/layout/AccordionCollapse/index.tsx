import Component from './component';
import { AccordionSizes, AccordionStyles } from './types';

Component.defaultProps = {
    size: AccordionSizes.Medium,
    style: AccordionStyles.Primary,
};

export default Component;
