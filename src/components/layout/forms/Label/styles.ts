import styled from 'styled-components';

export default styled.div`
    position: relative;
    padding-bottom: 1.15em;
    
    .label {
        font-size: 1.15em;
        color: #1c1c1c;
    }

    .label-required {
        color: #dd2222;
        padding-left: .25em;
    }
 
`;
