import styled from 'styled-components';
import variables from 'styles/vars';

export default styled.aside`
    .menu-categories {
        margin-top: 3em;
        
        .menu {
            .list-item {
                margin-top: .65em;
                padding: .4em 0;
                .name {
                    font-size: 1.15em;
                    font-weight: 300;
                    color: #666;
                }
            }
        }
    }
`;
