import React, { FunctionComponent } from 'react';

import StyledComponent from './styles';
import { Props } from './types';

const SideNewsElement: FunctionComponent<Props> = ({ post }) => {
    return (
        <StyledComponent className="module-blog-side-news-element">

        </StyledComponent>
    );
};

export default SideNewsElement;
